extends Node


signal screen_shake(amount, duration)

signal glitch(amount, duration)

signal booster_timer_update(percentage)

signal boosters_activated

signal bombs_activated

signal black_screen

signal clear_screen

signal health_increased(health)

signal game_saved

signal resize_camera(center_position, extents)

signal display_dialog_message(message)

signal clear_dialog_message()

signal display_message(upper, lower)

signal clear_message()

signal initiate_npc_interaction

signal end_npc_interaction

signal system_access_granted

signal select_weapon(weapon)

signal game_paused

signal gun_unlocked

signal gun_upgrade

signal zap_ray_upgrade