extends Area2D

# const effect = preload("res://Effects/PupEffect.tscn")

func _ready():
	pass

func pickup():
	# Utils.add_scene_to_main(effect, global_position)
	pass
	# $Sprite.visible = false
	# yield(get_tree().create_timer(5.5),"timeout")
	# queue_free()